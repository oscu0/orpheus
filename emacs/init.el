(setq EVIL_ENABLED nil)

(load (concat emacs-custom-path "evil.el"))

(load (concat emacs-custom-path "behavior.el"))

(load (concat emacs-custom-path "display.el"))

(load (concat emacs-custom-path "files.el"))

(load (concat emacs-custom-path "code.el"))

(load (concat emacs-custom-path "modes.el"))
